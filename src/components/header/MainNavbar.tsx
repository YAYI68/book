import React from 'react'

type Props = {}

const MainNavbar = (props: Props) => {
  return (
    <nav className='hidden h-full w-full lg:flex bg-white dark:bg-black dark:text-white border-b-2 dark:border-b-gray-500'>MainNavbar</nav>
  )
}

export default MainNavbar