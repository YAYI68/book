import { NotAvailable } from "@/components/notFound";

type Props = {};

const SavedBooks = (props: Props) => {
  return (
    <div>
      <NotAvailable />
    </div>
  );
};

export default SavedBooks;
