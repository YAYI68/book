import { BookForm } from "@/components/form";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div className="">
      <BookForm />
    </div>
  );
};

export default page;
